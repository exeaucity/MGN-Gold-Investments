# Webapp (Frontend + Backend + Postgres)


## Quick start
1. Install Git, Docker Desktop, VS Code, and the Dev Containers extension.
2. Copy `.env.example` to `.env` in the repo root.
3. Run:
```bash
docker compose up --build